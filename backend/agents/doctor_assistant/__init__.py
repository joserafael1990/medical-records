"""Doctor Assistant — read-only Gemini-powered chatbot for authenticated doctors.

Import sub-modules directly (`from agents.doctor_assistant import tools,
agent, ...`) rather than re-exporting from here, so a partial install
of this package (e.g. a test run that only exercises tools) doesn't
require the whole Vertex AI stack to import cleanly.
"""
